apiVersion: batch/v1
kind: Job
metadata:
  name: migrator
  namespace: {{ .Release.Namespace }}
  labels:
    app: migrator
  annotations:
    "helm.sh/hook": post-install, post-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  backoffLimit: 4
  template:
    metadata:
      labels:
        app: migrator
    spec:
      restartPolicy: Never
      containers:
        - name: migrator
          image: media-pipeline/migrator
          imagePullPolicy: IfNotPresent
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: database-secrets
                  key: url
      initContainers:
        - name: wait-for-postgres
          image: postgres:16-alpine
          command:
            - sh
            - -c
            - 'until pg_isready -h postgres-rw -p 5432 -U {{ .Values.postgres.username }}; do echo "waiting for postgres..."; sleep 2; done'